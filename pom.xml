<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.freese</groupId>
        <artifactId>maven-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath /> <!-- lookup parent from repository -->
    </parent>

    <groupId>de.freese</groupId>
    <artifactId>java</artifactId>
    <packaging>pom</packaging>

    <name>${project.groupId} - ${project.artifactId}</name>
    <description>Java-Projects</description>
    <inceptionYear>2022</inceptionYear>
    <url>https://github.com/tfreese/java</url>

    <organization>
        <name>Thomas Freese</name>
        <url>https://github.com/tfreese/java</url>
    </organization>

    <developers>
        <developer>
            <id>tfreese</id>
            <name>Thomas Freese</name>
            <url>${project.organization.url}</url>
            <roles>
                <role>developer</role>
                <role>architect</role>
            </roles>
            <timezone>+2</timezone>
        </developer>
    </developers>

    <scm>
        <url>${project.url}</url>
        <connection>scm:git:git://github.com/tfreese/java.git</connection>
        <developerConnection>scm:git:https://github.com/tfreese/java.git</developerConnection>
        <tag>HEAD</tag>
    </scm>

    <modules>
        <module>binding</module>
        <module>cellular-machines</module>
        <module>jsensors</module>
        <module>maven-proxy</module>
        <module>meta-model</module>
        <module>misc</module>
    </modules>

    <dependencies>
        <!-- compile scope -->
        <!-- runtime scope -->
        <!-- provided scope -->
        <!-- test scope -->
    </dependencies>

</project>
